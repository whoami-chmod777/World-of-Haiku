
Description
Capture the flag for dojo missions. Input without parameters is for flagged files. 
Example: flag flag_file_name.ext

Parameters

Service Version: -sV
Capture the service version. Use quotes if version contains whitespaces. 
Example: flag -sV "1.0.2 LTS"
Example: flag -sV "HTTP/1.1"

User Name: -u
Capture the user name of device. Use quotes if user name contains whitespaces. 
Example: flag -u "john_nichols_96"

Operating System: -oV
Capture the operating system version. Use quotes if version contains whitespaces. 
Example: flag -oV "22.04 LTS"

Services List: -sL
Capture the services list. Use quotes if service name contains whitespaces. Divide services name by whitespaces.
Example: flag -sL "TFTP" SSH "DHCP"
Example: flag -sL "Ssh" Nntp "Imap4" Upnp

String: -rS
Capture the random String flag. Use quotes if string contains whitespaces. 
Example: flag -rS "Hello world"
Example: flag -rS MyFlag
